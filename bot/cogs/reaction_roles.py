"""
reaction_roles.py — Reaction roles DISABLED / removed.
Commands (?reactionrole / ?rr) deleted by request.
"""
# Intentionally empty — no commands or background loops registered.


async def reaction_panel_post_loop():
    """No-op stub so main/events imports do not crash."""
    return
